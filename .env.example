# Create a Gemini API key in Google AI Studio, then copy this file to .env.
GEMINI_API_KEY=replace_with_your_api_key

# Model is configurable so the application can be updated without code changes.
GEMINI_MODEL=gemini-3.6-flash

# Server-side upload limit in MB.
MAX_FILE_SIZE_MB=10
